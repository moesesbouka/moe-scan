# Checkly UPC monitor

Create an API check that POSTs to /.netlify/functions/lookup with a known UPC and asserts status 200 and that the top title contains an expected keyword, for example `Flash` or `chair`.
